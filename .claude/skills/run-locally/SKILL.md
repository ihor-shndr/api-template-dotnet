---
name: run-locally
description: 'Start the Books API locally with dotnet run.'
---

# Run the App Locally

Run `.claude/skills/run-locally/assets/run-local.sh` to start the API.

App available at `http://localhost:5265`. Health check: `GET /api/health`.
